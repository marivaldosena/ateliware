version: '3'

services:
  web:
    build: './web'
    volumes:
      - '.:/app'
    ports:
      - '5000:5000'
    command: >
      gunicorn -b 0.0.0.0:5000
      --access-logfile -
      --reload
      --chdir ./web
      'app:create_app()'